[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "nidaqmx_on_pi"
version = "0.1.0"
description = "NI-DAQmx tools for Raspberry Pi (scaffold)"
requires-python = ">=3.8"

[tool.black]
line-length = 88
target-version = ['py38']
